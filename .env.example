# Copy to .env and adjust as needed

# Host address the MCP port is published on. Loopback by default: the endpoint has
# no authentication. Use a tunnel/VPN rather than widening this.
WITHINGS_MCP_BIND=127.0.0.1
WITHINGS_MCP_PORT=8586

TZ=Europe/Paris
WITHINGS_SYNC_DAYS=30
